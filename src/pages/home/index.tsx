import {theme} from "@/theme/theme";
import {Box} from "@mui/material";

const Home = () => {
    return (
        <Box sx={{color: theme.palette.primary.main}}>
            Home page
        </Box>
    );
};

export default Home;